# Handoff de sesión — 2026-05-29

Cierre de la semana de implementación R1. Snapshot para retomar mañana sin fricción.

---

## Lo que se hizo esta semana (resumen)

### Lun–Mar (2026-05-25 / 26)
- Foundation R0 efectivamente cerrado (excepto #12 MedGemma, bloqueado externamente).
- **ADR-0007**: Langfuse Cloud observability — operativa.
- Dataset longitudinal **Lucía Mendoza Quispe** (4 PDFs sintéticos, escenario DG).
- **ADR-0008**: Prompt registry Fase 1 — hashing inmutable.
- **ADR-0006** actualizado con validación empírica del plan Render.

### Mié (2026-05-27)
- **ADR-0009**: PHI redaction antes de Langfuse Cloud — implementado.
- **Provider routing** en `apps/api` (query param `?provider=anthropic|vertex`).
- **Sanitizer mejorado**: detección de keys-in-text (`nombre=Maria` → `nombre=[REDACTED]`).
- **Prompt `extract_obstetric_v2`** creado — corrige el bug "embarazo de N semanas en active_problems".
- API expone metadata operacional en response (`prompt_version`, `cost`, `tokens`, `trace_id`).

### Jue (2026-05-28)
- **Frontend Next.js completo**: auth con magic link, upload PDF, vista paciente longitudinal, comparador side-by-side.
- **ADR-0011**: stack frontend (Next.js + Supabase + Vercel) — aceptado.
- Frontend consume metadata del extractor + docs API actualizadas.

### Vie (2026-05-29) — hoy
- **Comparator offline de prompts**: paquete `evals/src/sica_evals/comparators/prompt_*.py` con verdict system GREEN/YELLOW/RED. Veredicto sobre Lucía v1 vs v2: **YELLOW** (cambios reclasificatorios, identidad preservada en 4/4).
- **Hash anchor para TODAS las versiones** de prompt en CI (v1 + v2). Test adicional detecta versiones nuevas sin anclar.
- **Mypy strict obligatorio** en CI (`|| true` removido — todos los paquetes ya pasaban strict con 0 errores).
- **ADR-0010**: política de retención Langfuse 180 días + paquete `scripts/langfuse_retention/` + workflow `.github/workflows/langfuse-cleanup.yml`.
- Índice de ADRs actualizado, snapshot `docs/STATUS.md` creado, playbook de activación escrito.

---

## Estado del repo al cierre

- **Branch:** `main`.
- **Working tree:** clean.
- **Commits hoy (2026-05-29):** 5 — `9c9b1ac`, `265e482`, `aa98a97`, `a96f007`, `1e8e441` + el handoff que sigue.
- **Tests totales:** 115 (evals) + 141 (clinical-extractor sin tracing pre-existente) + 23 (langfuse_retention) = **279 pasando localmente**. Tests de `apps/api` cuentan aparte y CI los corre.
- **Lint:** `ruff check` clean en todos los paquetes Python.
- **Type-check:** `mypy --strict` clean en `clinical-extractor`, `evals`, `apps/api`, `langfuse_retention`.
- **ADRs:** 0001–0011 (índice actualizado).
- **Documentación operacional:** `phi-handling.md`, `langfuse-retention.md`, `frontend-deploy.md`, `activation-playbook-2026-05-29.md`, `handoff-2026-05-29.md`.
- **Producción:**
  - Render `sica-api` deployed y funcional.
  - Vercel `sica-web` deployed pero **esperando env vars de Supabase** (Paso 5 del playbook).

---

## Bloqueos al cierre

### Activación pendiente (mañana)

Ver [docs/operations/activation-playbook-2026-05-29.md](activation-playbook-2026-05-29.md):

- Crear Supabase project `sica-platform`.
- Aplicar SQL migration (`supabase/migrations/0001_initial_schema.sql`).
- Configurar Supabase auth (Site URL, Redirect URLs, Email provider).
- Agregar 4 env vars a Vercel.
- Agregar 3 secrets a GitHub Actions (Langfuse cleanup).
- Smoke E2E con Lucía (sem16 + sem24 + timeline + comparador).

**Tiempo estimado:** 25–35 min.

### Decisiones pendientes

- **Promover prompt `v2` → default**:
  - Veredicto del comparator: **YELLOW** (no autoriza promoción automática).
  - Requiere revisión clínica humana sobre 1–2 casos reales del médico colaborador.
  - Recomendación: esperar al primer uso real antes de promover.

### Externamente bloqueado

- **#12 MedGemma 4B** (GCP billing del partner).
- **#1 Regulatorio** (clasificación software asistivo).
- **#2 Ley 29733** (DPIA + DPO + consentimientos).
- **#3 Marca Indecopi**.
- **#4 Partner fundador clínico**.
- **#5 150–200 historias obstétricas para R0**.

---

## Sin trabajo abierto

- No hay archivos modificados sin commit.
- No hay branches pendientes (todo se mergeó directo a main por el modo autónomo de la semana).
- No hay tests rotos.
- No hay TODOs urgentes que no estén ya capturados en `docs/STATUS.md`.

---

## Lo primero mañana

1. Abrir Claude.ai → mensaje tipo: "Buenos días, ejecutemos el activation playbook".
2. Yo (o la sesión que tomes) te guía paso a paso por `docs/operations/activation-playbook-2026-05-29.md`.
3. Si todo OK al final del playbook: **invitar al médico colaborador**.
4. Si emerge algún bug del smoke E2E: documentar en `docs/operations/bug-2026-05-30.md` y decidir rollback o forward fix.

---

## Tu cierre del día hoy

```bash
git status                  # → clean
git log --oneline -10       # → últimos commits visibles
git branch                  # → solo main
```

Cierra Claude Code (`/exit` o cerrar terminal). Descansa.

---

## Referencias

- [docs/STATUS.md](../STATUS.md) — snapshot del estado R1.
- [docs/operations/activation-playbook-2026-05-29.md](activation-playbook-2026-05-29.md) — pasos del sábado.
- [docs/decisions/README.md](../decisions/README.md) — índice de ADRs actualizado.
- [MASTER_PLAN.md](../../MASTER_PLAN.md) — auto-generado por `sync-roadmap.yml` desde issues.
